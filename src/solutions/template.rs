use std::cmp;

pub fn solve_one(input: Vec<String>) -> String {
    "".to_string()
}

pub fn solve_two(input: Vec<String>) -> String {
    "".to_string()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_solve_one() {
        assert!(true);
    }

    #[test]
    fn test_solve_two() {
        assert!(true);
    }
}